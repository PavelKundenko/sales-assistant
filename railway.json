{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "NODE_ENV=production npm run migration:run:prod && npm run start:prod",
    "restartPolicyType": "ON_FAILURE"
  }
}
